# # This file was auto-generated by the Firebase CLI
# # https://github.com/firebase/firebase-tools

# name: Deploy to Firebase Hosting on PR

# on:
#   pull_request

# permissions:
#   checks: write
#   contents: read
#   pull-requests: write

# jobs:
#   build_and_preview:
#     if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Repository
#         uses: actions/checkout@v4

#       - name: Install Dependencies
#         run: npm ci --legacy-peer-deps  # Fixes dependency conflicts

#       - name: Build Project
#         run: npm run build

#       - name: Install Firebase CLI
#         run: npm install -g firebase-tools

#       - name: Deploy to Firebase Hosting Preview
#         uses: FirebaseExtended/action-hosting-deploy@v0
#         with:
#           repoToken: ${{ secrets.GITHUB_TOKEN }}
#           firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_APTOS_WALLET_ADAPTER_F0A61 }}
#           projectId: aptos-wallet-adapter-f0a61
#           preview-channel: pr-${{ github.event.pull_request.number }} # Unique preview channel per PR
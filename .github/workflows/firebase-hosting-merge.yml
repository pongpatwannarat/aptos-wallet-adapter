# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

# name: Deploy to Firebase Hosting on merge
# on:
#   push:
#     branches:
#       - main
# jobs:
#   build_and_deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Repository
#         uses: actions/checkout@v4
#         with:
#           fetch-depth: 0  # Ensures all history is fetched for proper builds

#       - name: Install Dependencies
#         run: npm ci --legacy-peer-deps  # Fixes dependency conflict

#       - name: Build Project
#         run: npm run build

#       - name: Install Firebase CLI
#         run: npm install -g firebase-tools

#       - name: Deploy to Firebase Hosting
#         uses: FirebaseExtended/action-hosting-deploy@v0
#         with:
#           repoToken: ${{ secrets.GITHUB_TOKEN }}
#           firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_APTOS_WALLET_ADAPTER_F0A61 }}
#           channelId: live
#           projectId: aptos-wallet-adapter-f0a61
